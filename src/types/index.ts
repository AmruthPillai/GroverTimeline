export type EventType = {
  date: string;
  title: string;
  location: string;
  summary: string;
  image: string;
};

export type StatisticType = {
  number: number;
  more?: boolean;
  label: string;
};
